@model ECOMMERCE.CORE.EntityFormaPago
@{
    ViewBag.Title = "Editor Forma de Pago";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="row justify-content-center m-3">
    <div class="col-sm-8">
        <div class="card">
            <div class="card-header text-center">
                <h1>FORMA DE PAGO</h1>
            </div>

            @using (Html.BeginForm("Delete", "MasterFormasPago", FormMethod.Post))
            {
                <div class="card-body text-left">
                    @Html.ValidationSummary(true)
                    @Html.HiddenFor(model => model.ididentifier_i)

                    <div class="form-group">
                        ¿Quieres borrar @Model.ididentifier_i @Model.name_nv de la base de datos?
                    </div>

                </div>

                <div class="card-footer text-center">
                    <a class="btn btn-primary" asp-area="" asp-controller="MasterFormasPago" asp-action="Index">Volver a Formas de Pago</a>
                    <input class="btn btn-primary"  type="submit" value="Borrar" />
                    
                </div>

            }

            
            
        </div>
    </div>
</div>